import React from 'react';
import buildIcon from '../Assets/Icons/icons8-support-24.png';

function BuildIcon(props) {
  return (
    <img src={buildIcon} alt='BP' className='icon'/>
  );
}

export default BuildIcon;